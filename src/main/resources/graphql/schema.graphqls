type Query {
    personById(id: ID!): Person
    allPersons: [Person]
}

type Person {
    id: ID!
    name: String
    lastName: String
    birthDate: String
    phoneNumber: String
    address: Address
}

type Address {
    id: ID!
    street: String
    number: String
    postalCode: String
}